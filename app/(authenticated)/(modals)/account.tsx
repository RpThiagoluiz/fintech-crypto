import { View, Text } from 'react-native';

import { defaultStyles } from '@/constants/Styles';

export const Page = () => (
   <View style={defaultStyles.container}>
      <Text style={defaultStyles.header}>Account PAGE</Text>
   </View>
);

export default Page;
